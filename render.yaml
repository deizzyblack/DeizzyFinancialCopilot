databases:
  - name: financial-copilot-db
    plan: free
    databaseName: financial_autopilot

services:
  - type: web
    name: financial-copilot-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && mkdir -p uploads
    startCommand: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FDA_DATABASE_URL
        fromDatabase:
          name: financial-copilot-db
          property: connectionString
      - key: PYTHON_VERSION
        value: "3.11.11"
      - key: FDA_UPLOAD_DIR
        value: "/tmp/uploads"
